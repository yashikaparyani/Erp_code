TENDER → (WON) → PROJECT
    ↓
SURVEY (per site) → Survey COMPLETE?
    ↓ YES
BOQ + COSTING → Approval Workflow
    ↓ APPROVED
INDENT → RFQ → 3-QUOTE → COMPARISON → PO
    ↓
GRN (partial deliveries) → SERIAL TRACKING → DISPATCH
    ↓
SITE INSTALLATION → DEPENDENCY CHECK → COMMISSIONING
    ↓
MILESTONE BILLING → INVOICE → PAYMENT TRACKING
    ↓
O&M TICKETING → SLA TIMER → RMA

Sprint 1: Foundation & Standardization (Week 1)
Goal: Fix existing DocTypes, establish patterns

Backend Tasks:

Audit all 30 existing DocTypes - fix naming conventions (GE_ prefix consistent)
Create base DocTypes:
GE Organization (company settings)
GE Branch
GE Department
Setup Approval Workflow engine (Frappe built-in):
approval_workflows DocType
approval_instances DocType
approval_actions DocType
Create ge_audit_log for all critical changes
Standardize API pattern:
/api/method/gov_erp.api.{module}.{function}

Deliverables:

Clean DocType structure
Working approval workflow framework
Audit logging enabled
Sprint 2: Tender → Project Conversion (Week 2)
Goal: Complete tendering module, enable project creation

Backend Tasks:

GE Tender enhancements:

Add workflow_state (DRAFT → SUBMITTED → WON/LOST)
Add linked_project field
Add conversion validation (Can't convert unless WON)
EMD/PBG Tracker APIs:
/emd-pbg/list
/emd-pbg/create
/emd-pbg/update-status (ACTIVE/RELEASED/FORFEITED)
/emd-pbg/expiry-alerts

Tender → Project API:
/tender/{id}/convert-to-project
→ Validates: status=WON, EMD released, BOQ exists
→ Creates: Project + Sites (from BOQ sites)

Business Rules:

Tender status progression: DRAFT → SUBMITTED → TECHNICAL_EVAL → FINANCIAL_EVAL → WON/LOST
Only WON tenders can convert to Project
EMD status tracked separately
Sprint 3: Survey + BOQ Module (Week 3)
Goal: Survey gates BOQ approval (dependency rule #1)

Backend DocTypes:

GE Survey:

tender_id/project_id, site_id, survey_date
surveyed_by, status (PENDING/IN_PROGRESS/COMPLETED)
photos[], site_notes
GE Item (Master):

item_code, name, category, unit
is_serialized, gst_rate, hsn_sac
GE BOQ (Header):

tender_id/project_id, version, status
created_by, approved_by, approved_at
GE BOQ Line (Child):

boq_id, site_id, item_id (optional)
description, qty, unit, rate, amount
make, model
GE Cost Sheet + GE Cost Line:

Margin-on-cost calculation
Version tracking
APIs:
/survey/create
/survey/complete  → Updates status + triggers BOQ unlock
/boq/create
/boq/add-line
/boq/submit-for-approval
/boq/approve  → CHECK: All surveys completed? If NO → Block
/costing/calculate-margin

Business Rule: BOQ.approve() must verify Survey.status = COMPLETED for ALL linked sites

Sprint 4: Procurement Part 1 (Week 4)
Goal: Vendor management, Indent creation, RFQ flow

Backend DocTypes:

GE Party (Vendor/Client):

type (CLIENT/VENDOR/BOTH)
name, gstin, pan
addresses[], contacts[]
GE Indent (Material Request):

project_id, site_id
requested_by, requested_on
required_by_date, status, approved_by
GE Indent Line:

indent_id, item_id, qty, notes
GE RFQ (Request for Quotation):

indent_id, created_by, status
vendors[] (list of vendors sent to)
GE Vendor Quotation + GE Vendor Quotation Line:

rfq_id, vendor_id, quote_date
validity_date, total_amount, status
APIs:

Sprint 5: Procurement Part 2 (Week 5)
Goal: PO creation, 3-Quote compliance

Backend DocTypes:

GE Comparison Sheet:

indent_id, prepared_by, status
exception_approved_by, exception_reason
GE Purchase Order (PO):

po_number, vendor_id, project_id
indent_ids[], po_date, status
payment_terms, delivery_terms, revision_no
GE PO Line:

po_id, item_id, qty, rate, tax, amount
APIs:

Business Rule:

PO creation blocked unless 3 quotations OR exception approved
One PO can cover multiple indents
Indent lines can split across multiple POs
Sprint 6: Stores & Inventory (Week 6)
Goal: GRN, Serial tracking, Dispatch, Stock ledger

Backend DocTypes:

GE Store:

name, branch_id, project_id
type (HO/PROJECT/SITE_TEMP)
GE GRN (Goods Receipt Note):

po_id, received_at_store, received_on, status
GE GRN Line:

grn_id, item_id
qty_received, qty_accepted, qty_rejected
GE Serial Number:

item_id, serial_no
current_status (IN_STOCK/ISSUED/INSTALLED/RMA/SCRAP)
current_location{type, id}, source_grn_line
GE Dispatch Challan + GE Dispatch Line:

from_store, to_store/to_site
serial_ids[]
GE Stock Ledger:

item_id, store_id
movement_type, qty_in, qty_out
ref_type, ref_id, timestamp
APIs:

Business Rule:

NO negative stock allowed
Serialized items (cameras, servers) mandatory serial tracking
All movements create ledger entry
Sprint 7: Project Execution (Week 7)
Goal: Projects, Sites, Milestones, Tasks, DPR

Backend DocTypes:

GE Project Type:

name (ITMS/Surveillance/Solar/O&M/AMC)
milestone_templates[], task_templates[]
GE Project:

project_code, name, project_type_id
client_id, branch_id, region
start_date, planned_end_date, status
contract_summary{value, retention_percent, ld_rules}
GE Site:

project_id, site_code, name
address, lat, lng, status
GE Project Team:

project_id, user_id, role_in_project
GE Milestone:

project_id, site_id, name
planned_date, actual_date, status
GE Task:

project_id, site_id, milestone_id
name, owner_user_id
planned_start, planned_end
actual_start, actual_end, status
GE DPR (Daily Progress Report):

project_id, site_id, report_date
summary, created_by, photos[]
APIs:

Sprint 8: Dependency Engine (Week 8) ⭐ CORE
Goal: Block task progression if prerequisites incomplete

Backend DocTypes:

GE Dependency Rule:

task_id
prerequisite_type (TASK/MATERIAL/DOCUMENT/APPROVAL/IP/SURVEY)
prerequisite_ref_id
hard_block (true/false)
GE Dependency Override:

task_id, override_by
approved_by, reason, timestamp
Dependency Engine Logic:

APIs:

Business Rules:

Hard block = Cannot proceed without override
Soft block = Warning only
Override creates permanent audit record
Sprint 9: Billing & Finance (Week 9)
Goal: Invoice tracking, Payments, Retention, Penalties

Backend DocTypes:

GE Invoice:

project_id, site_id, invoice_no
invoice_type (MILESTONE/RA/O&M)
amount, gst_breakup, status
milestone_complete (boolean flag for audit)
GE Invoice Line:

linked_entity_type, linked_entity_id
description, qty, rate, amount
GE Payment Receipt:

invoice_id, received_date
amount_received, tds_amount, remarks
GE Retention Ledger:

project_id, invoice_id
retention_amount, release_due_date
released_on, status
GE Penalty Deduction:

project_id, invoice_id
source (SLA/LD/OTHER)
amount, reason, applied_on
APIs:

Sprint 10: O&M + Alerts (Week 10)
Goal: Ticketing, SLA Timer, Notifications

Backend DocTypes:

GE Ticket:

ticket_no, project_id, site_id, asset_id
category, priority, status
raised_by, raised_on, assigned_to
resolved_on, closed_on
GE Ticket Action:

ticket_id, action_type (COMMENT/ASSIGN/VISIT/START/PAUSE/RESOLVE/CLOSE)
by_user_id, timestamp, notes
GE SLA Profile:

contract_id, response_minutes, resolution_minutes
working_hours (24x7)
GE SLA Timer:

ticket_id, started_on
paused_intervals[], total_pause_minutes
closed_on
GE SLA Penalty Rule + GE SLA Penalty Record

GE Alert Rule + GE Alert Instance + GE Notification Log

APIs:

Verification
Tender Flow: Create tender → Submit → Win → Convert to Project
Survey Gate: Try BOQ approve without survey → Should block
3-Quote Rule: Try PO without 3 quotes → Should block
Dependency Block: Try task start without prerequisite → Should show blocker
SLA Timer: Create ticket → Verify timer runs 24x7
Decisions Made
Database: MariaDB (Frappe native) - NOT MongoDB
Approval Engine: Using Frappe built-in workflows
API Pattern: /api/method/gov_erp.api.{module}.{function}
Serial Tracking: Mandatory for cameras, servers, network devices
SLA: 24x7x365, pause only by authorized roles
Further Considerations
Engineering Module - Drawings, deviation logs - implement after core flow? (Recommend: defer to Sprint 11)
Network/Commissioning - IP Pool, Device Register - part of Sprint 7 or separate? (Recommend: Sprint 7.5)
Reports/Analytics - Dashboard APIs - Sprint 10 or dedicated sprint? (Recommend: Sprint 11)